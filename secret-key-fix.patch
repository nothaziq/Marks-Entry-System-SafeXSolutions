diff --git a/backend/README.md b/backend/README.md
index 254dd08..eefba37 100644
--- a/backend/README.md
+++ b/backend/README.md
@@ -70,8 +70,10 @@ backend/
 
 Requires Docker + Docker Compose installed.
 
+`docker-compose.yml` lives at the **repo root**, not in `backend/`. From the repo root:
+
 ```bash
-cd backend
+cp .env.example .env        # then fill in SECRET_KEY, e.g. via: openssl rand -hex 32
 docker compose up --build
 ```
 
diff --git a/docker-compose.yml b/docker-compose.yml
index d678112..14a2d4b 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -28,7 +28,7 @@ services:
         condition: service_healthy
     environment:
       DATABASE_URL: postgresql://attendance_user:attendance_pass@db:5432/attendance_db
-      SECRET_KEY: change-this-to-a-long-random-string
+      SECRET_KEY: ${SECRET_KEY:?Set SECRET_KEY in a .env file at the project root — see .env.example}
       JWT_ALGORITHM: HS256
       ACCESS_TOKEN_EXPIRE_MINUTES: 60
       CORS_ORIGINS: http://localhost:5173
