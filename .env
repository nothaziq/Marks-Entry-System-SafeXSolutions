# Read by `docker compose` for variable substitution in docker-compose.yml.
# Copy this to `.env` in the project root and fill in a real value —
# `docker-compose.yml` will refuse to start without it.
#
# Generate one with: openssl rand -hex 32

SECRET_KEY=0e46cd0ae374175eb6dcba225cdbee3a89f5d6d4545ea3e1e2ae77784cd1d97e
